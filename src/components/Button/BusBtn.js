import React from "react";
import "./Button.css";

const BusBtn = ({ type="default", className, children, onClick}) => (
    <button>
        Business Services
    </button>
);

export default BusBtn;